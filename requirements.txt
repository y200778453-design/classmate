# ClassMate 課堂智聽 — dependencies
PySide6>=6.6
requests>=2.28
vosk>=0.3.45        # offline ASR (zh / en); optional
sounddevice>=0.5    # microphone capture; optional
SpeechRecognition>=3.10  # online Google STT (Cantonese zh-HK); optional
pytest>=8.0         # dev/test only
